"""Evaluation harness for ECHO memory extraction.

Measurement only. Nothing here modifies the `echo` package, and no result is fed
back into ECHO — the harness reads the shipped extraction prompt and reports how
it did. ECHO does not learn from running this.
"""
